# RequireJS compatibility check

This test is not automated yet. Follow this:

```bash
$ cd test/requirejs
$ bower install
$ cd ../../
$ ss
```

Then open the url (like `http://localhost:3474/test/requirejs/`).
It's OK if there's "Hello RequireJS!" message on the console.
